---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.0
  name: providerconfigs.pgbeam.io
spec:
  group: pgbeam.io
  names:
    categories:
    - crossplane
    kind: ProviderConfig
    listKind: ProviderConfigList
    plural: providerconfigs
    singular: providerconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProviderConfig configures how the provider connects to the PgBeam
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProviderConfigSpec defines the desired state of the provider
              configuration.
            properties:
              apiKeySecretRef:
                description: |-
                  APIKeySecretRef is a reference to a Secret containing the PgBeam API key.
                  The key must be stored under the "apiKey" data field.
                properties:
                  key:
                    description: The key to select.
                    type: string
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - key
                - name
                - namespace
                type: object
              baseUrl:
                default: https://api.pgbeam.com
                description: BaseURL is the PgBeam API base URL. Defaults to https://api.pgbeam.com.
                type: string
            required:
            - apiKeySecretRef
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
